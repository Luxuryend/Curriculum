## 1-02 试简述分组交换的要点
分组交换的核心机制是存储转发  
优点：高效、灵活、迅速、可靠  
缺点：时延、开销

## 1-22 网络协议的三个要素是什么？各有什么含义？
语法：数据与控制信息的结构或格式  
语义：需要发出何种控制信息，完成何种动作以及做出何种响应  
同步：规定了事件实现的顺序以及速度匹配

## 2-10 常用的传输媒体有哪几种？各有何特点？
**导引型传输媒体**：  
双绞线：便宜  
同轴电缆：抗干扰能力强  
光纤：带宽高，损耗低  
**非导引型传输媒体**：  
无线电：穿透能力强

## 2-13 为什么要使用信道复用技术？常用的信道复用技术有哪些？
信道复用技术能提高利用率，降低成本  
常用信道复用技术有：频分复用、时分复用、波分复用、码分复用

## 3-18 试说明10BASE-T
“10”代表数据传输速率为10Mbps  
“BASE”代表信号是基带信号  
“T”代表双绞线

## 3-20 假定 1 km 长的 CSMA/CD 网络的数据率为 1 Gbit/s。设信号在网络上的传播速率为 200000 km/s。能够使用此协议的最短帧长。

最短帧长 = 数据传输率 * 2 * 传播时延  
传播时延 = 信道长度 / 传播速率  

所以最短帧长 = 1 Gbit/s * 2 * 1km / (200000 km/s) = 10^9 / 10^5 = 10000 bit

## 3-22 假定在使用 CSMA/CD 协议的 10 Mbit/s 以太网中某个站在发送数据时检测到碰撞，执行退避算法时选择了随机数 r=100。试问这个站需要等待多长时间后才能再次发送数据？如果是 100 Mbit/s 的以太网呢？

等待时间 = r * 争用期  
争用期固定为 512 比特时间

对于 10 Mbit/s 以太网
争用期 = 512 / 10 * 10^6 = 51.2 μs  
等待时间 = 100 * 51.2 μs = 5120 μs  

对于100 Mbit/s  
则是 512 μs  

## 4-15 一个3200位长的TCP报文到IP层，加上160位的首部后成为数据报。下面的互联网由两个局域网通过路由器连接起来，但第二个局域网所能传送的最长数据帧中的数据部分只有1200位。因此数据报在路由器必须进行分片。试问第二个局域网向其上层要传送多少比特的数据（这里的 “数据” 当然指的是局域网看见的数据）？

1200 - 160 = 1040 （1040需要是8的倍数，不是的话需要向下找）  
每次只能穿1040个数据，所以分别是 1040 1040 1040 80  
结果 = 1200 * 3 + 80 + 160 = 3840

## 4-18 路由转发练习题

设某路由器建立了如下转发表：

| 前缀匹配 | 下一跳 |
| :--- | :--- |
| 192.4.153.0/26 | $R_3$ |
| 128.96.39.0/25 | 接口 m0 |
| 128.96.39.128/25 | 接口 m1 |
| 128.96.40.0/25 | $R_2$ |
| 192.4.153.0/26 | $R_3$ |
| *（默认） | $R_4$ |

现共收到 5 个分组，其目的地址分别为：

1. **128.96.39.10**
2. **128.96.40.12**
3. **128.96.40.151**
4. **192.4.153.17**
5. **192.4.153.90**

**试分别计算其下一跳。**

## 4-19 某单位分配到一个地址块 $129.250/16$。该单位有 4000 台机器，平均分布在 16 个不同的地点。试给每一个地点分配一个地址块，并算出每个地址块中 IP 地址的最小值和最大值。


