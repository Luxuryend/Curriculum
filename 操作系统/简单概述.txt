第一章
操作系统是什么
操作系统特征并发、共享、虚拟、异步性
操作系统五大功能处理机管理、
用户接口命令接口、程序接口、图像接口
操作系统的新能指标五个 系统的可靠性、系统吞吐率、系统的响应时间、系统资源的
操作系统的分类批处理操作系统、分时操作系统、实时操作系统、嵌入式、个人计算机、网络、分布式
分时、实时需要知道优缺点、分时的特征（及时性）

第二章
进程前趋图一定考（）
进程的五个基本特征
进程的三个基本状态、新建、完成、静态就绪、静态阻塞
进程控制块（PCB）描述
进程同步（使用临界区的原则（有空让进、无空等待、多中择一、有限等待、让权等待））
竞争使用资源（使用共同的信号量）、同步（empty（初始1）和full（初始0））第一步P(E)V(F)第二部P（F）V(E)
生产者消费者问题（合作问题）（一个生产者一个消费者一个缓冲区（empty（初始1）和full（初始0）））（多个缓冲区一个生产者一个消费者（in、out分别指向空缓冲和满缓冲））（多个生产者多个消费者多个缓冲区（mutex互斥信号量初值1）互斥使用缓冲区P(E）P(M)    V(M)    V(F)）
读者写者问题（两个信号量、w、mutex、readcount读者计数）读者 Pm  Pw Vm 读  Pm Vw Vm
写者   Pw  写 Vw



第三章
处理机三级调度，高级调度、中级（挂起和解除）、低级（进程）
作业的状态的转换（提交状态（需要提交作业说明书）-后备状态（JCB）-运行状态（PCB）-完成状态（资源的释放和回收））
进程调度的算法和评价（先来先服务、时间片轮转、优先级调度、多级队列）周转时间（完成时间-到达时间）
作业控制块（JCB）
作业调度算法（先来先服务、短作业优先、最短剩余时间、高响应比（百分百考）（响应比=1+等待时间/要求运行时间））
死锁的概念、产生死锁的四个必要同时具备条件（互斥条件、保持请求（部分分配条件）、不剥夺条件、环路等待条件）预防死锁只能破坏后面三个
银行家算法（安全状态：如果能在有限的时间内，保证所有进程都得到自己所需的全部资源，就称系统处于安全状态）（考试一定要画表格、写过程为什么处于安全状态或不安全状态）


第四章
逻辑地址空间（从0地址开始）
地址重定位（静态重定位（一次性实现逻辑到物理的转换）、动态重定位（在访问过程中进行逻辑到物理的转换））
分区存储管理（单一分区（所有的内存都分配给一个用户、不支持多用户）、固定分区（分为用户区和系统区、分为若干个固定大小的区、要有分区分配表）、动态分区（空闲区表、已分配区表））、分配的都是连续分区
三种分配算法
页式存储管理（逻辑地址分页、物理地址分块）（依赖页表寄存器：保存页的起始地址和页的长度，长度判断地址溢出）
地址的映射（一定要有页表（页号和块号））（逻辑地址二元组，有页号和页内地址组成）
快表
二级和多级页表（因为页表太大了，占用了相当大的内存空间）
分段式存储（段的长短的划分是由程序逻辑确定的，不是固定大小）（需要段表（记录长度和首地址））
段页式存储（把段里再化成页）（此时物理地址是按页管理）
虚拟存储（页表分为（页号、中断位、内存块号、外存地址、访问位、修改位））
页面淘汰算法（先进先出（FIFO）、最近最少未使用（LRU）、最佳页面淘汰（OPT）


第五章
磁盘调度
四种（先来先服务（FCFS）、最短服务时间（SSTF）、扫描（电梯）策略（SCAN）、循环扫描（单向扫描）（CSAN）），算寻道长度和平均寻道长度
