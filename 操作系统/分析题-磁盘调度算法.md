# 题目

某移动臂磁盘的柱面由外向里从0开始顺序编号,假设当前磁头停在100号柱面而且移动方向是向外的,现有一个请求队列在等待访问磁盘,访问的柱面号分别为190、10、160、90、80、125、30、20、140和25。请给出分别采用最短寻道时间优先和电梯调度算法处理上述请求的次序,并分别计算出它们的平均寻道长度。

# 解答

最短寻道时间优先就是谁距离短去谁那里

电梯调度算法 向外就是往0的方向走，但不走到端点处

平均寻道长度就是所有差值加起来除以数值个数（不包含当前位置）
